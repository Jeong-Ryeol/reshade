# SHERBET Plan 5 — OSD Customization Implementation Plan

> REQUIRED SUB-SKILL: subagent-driven-development. Steps use `- [ ]`.

**Goal:** FPS 배지(OSD)를 화면 어디든 자유 배치(X/Y 비율)·크기 조절·마우스 드래그 이동 가능하게 한다. 사용자의 명시 요청.

**Architecture:** 코너 기반 `_fps_pos` 위치 로직을 자유 좌표(0~1 비율) `_sherbet_osd_x/y`로 교체. 설정 탭에 X/Y/크기 슬라이더. 오버레이 열림 시 OSD 창을 드래그 가능하게 하고 위치를 비율로 저장.

## Global Constraints

- ImGui 1.92.5 + IMGUI_DISABLE_OBSOLETE_FUNCTIONS. obsolete API 금지.
- runtime_gui.cpp Mac 빌드 불가 → 정독 + CI. 핵심 로직(FPS 계산/표시 내용) 보존, 위치·크기만.
- 외부 라이브러리 금지. 커밋 메시지 Claude/AI 문구 금지.
- 검증: 플랜 끝 push 1회 + CI green + 시각 확인 권장.
- 범위: OSD 커스텀만. 즐겨찾기 필터·비교 슬라이더는 후속(YAGNI, 이번 제외).

---

### Task 1: OSD 자유 위치(X/Y) + 크기 + 설정 슬라이더

**Files:** Modify `source/runtime.hpp`, `source/runtime_gui.cpp`

**Interfaces:**
- Produces: 멤버 `float _sherbet_osd_x = 0.02f;` `float _sherbet_osd_y = 0.02f;` (화면 비율 0~1). 설정 `OVERLAY/SherbetOsdX,SherbetOsdY`.

- [ ] **Step 1: 멤버 추가 (runtime.hpp)**

`_sherbet_tab` 근처(또는 `_fps_scale` 근처)에 추가:
```cpp
		float _sherbet_osd_x = 0.02f; // OSD 가로 위치 (0=왼쪽,1=오른쪽)
		float _sherbet_osd_y = 0.02f; // OSD 세로 위치 (0=위,1=아래)
```

- [ ] **Step 2: 설정 로드/저장**

로드부(`config.get("OVERLAY", "FPSPosition", _fps_pos);` 근처)에 추가:
```cpp
	config.get("OVERLAY", "SherbetOsdX", _sherbet_osd_x);
	config.get("OVERLAY", "SherbetOsdY", _sherbet_osd_y);
```
저장부(`config.set("OVERLAY", "FPSPosition", _fps_pos);` 근처)에 추가:
```cpp
	config.set("OVERLAY", "SherbetOsdX", _sherbet_osd_x);
	config.set("OVERLAY", "SherbetOsdY", _sherbet_osd_y);
```

- [ ] **Step 3: 위치 로직 교체 (OSD 렌더 블록)**

`source/runtime_gui.cpp`의 OSD 위치 계산 부분:
```cpp
		if (_fps_pos % 2)
			fps_window_pos.x = imgui_io.DisplaySize.x - fps_window_size.x - 5;
		if (_fps_pos > 1)
			fps_window_pos.y = imgui_io.DisplaySize.y - fps_window_size.y - 5;
```
를 다음으로 교체:
```cpp
		// SHERBET: 자유 X/Y 배치 (화면 비율)
		fps_window_pos.x = _sherbet_osd_x * ImMax(0.0f, imgui_io.DisplaySize.x - fps_window_size.x);
		fps_window_pos.y = _sherbet_osd_y * ImMax(0.0f, imgui_io.DisplaySize.y - fps_window_size.y);
		const bool osd_align_right = _sherbet_osd_x > 0.5f;
```
그리고 이 OSD 창 블록 안에서 텍스트 정렬에 쓰이는 `_fps_pos % 2`(4곳, 대략 이어지는 show_clock/show_fps/show_frametime/show_preset_name 블록)를 모두 `osd_align_right`로 교체. (해당 블록은 OSD `ImGui::Begin("OSD"...)`와 그 `ImGui::End()` 사이에만 있음. 다른 함수의 `_fps_pos`는 건드리지 말 것 — grep로 범위 확인.)

- [ ] **Step 4: 설정 UI — X/Y 슬라이더 추가, 코너 콤보 제거**

`source/runtime_gui.cpp` draw_gui_settings의 OSD 섹션:
```cpp
			modified |= ImGui::SliderFloat(_("OSD text size"), &_fps_scale, 0.2f, 2.5f, "%.1f", ImGuiSliderFlags_AlwaysClamp);
			modified |= ImGui::ColorEdit4(_("OSD text color"), _fps_col, ImGuiColorEditFlags_AlphaBar);
```
바로 아래(그리고 기존 `OSD position on screen` 콤보 라인을 아래 슬라이더로 교체):
```cpp
			modified |= ImGui::SliderFloat("OSD X", &_sherbet_osd_x, 0.0f, 1.0f, "%.2f", ImGuiSliderFlags_AlwaysClamp);
			modified |= ImGui::SliderFloat("OSD Y", &_sherbet_osd_y, 0.0f, 1.0f, "%.2f", ImGuiSliderFlags_AlwaysClamp);
```
`OSD position on screen` 콤보 라인(`ImGui::Combo(_("OSD position on screen"), reinterpret_cast<int *>(&_fps_pos), ...)`)과 그 위의 `fps_pos_items` 준비 코드는 제거(또는 콤보만 제거). `_fps_pos` 멤버 자체는 남겨도 무방(설정 호환). 제거로 인해 `fps_pos_items` 지역변수가 다른 곳에서 안 쓰이는지 확인.

- [ ] **Step 5: 정독 확인 + Commit(push 안 함)**

`grep -n '_fps_pos' source/runtime_gui.cpp` → OSD 렌더 블록(1240~1303)과 설정에서 `_fps_pos % 2`가 남지 않았는지, 다른 함수의 `_fps_pos`는 그대로인지 확인. 중괄호 균형.
```bash
cd ~/reshade
git add source/runtime.hpp source/runtime_gui.cpp
git commit -m "feat(sherbet): OSD 자유 X/Y 위치 + 크기 슬라이더"
```

---

### Task 2: OSD 마우스 드래그 이동

**Files:** Modify `source/runtime_gui.cpp` (OSD 창 블록)

**설명:** 오버레이가 열려 있을 때만 OSD 창을 드래그 가능하게 하고, 놓으면 위치를 비율로 저장.

- [ ] **Step 1: OSD 창 플래그를 오버레이 상태에 따라 분기**

OSD `ImGui::Begin("OSD", nullptr, ...flags...)`에서 `ImGuiWindowFlags_NoMove | ImGuiWindowFlags_NoInputs`를 오버레이 열림 시 제거한다. 즉:
```cpp
		ImGuiWindowFlags osd_flags =
			ImGuiWindowFlags_NoDecoration |
			ImGuiWindowFlags_NoNav |
			ImGuiWindowFlags_NoSavedSettings |
			ImGuiWindowFlags_NoDocking |
			ImGuiWindowFlags_NoFocusOnAppearing |
			ImGuiWindowFlags_NoBackground |
			ImGuiWindowFlags_AlwaysAutoResize;
		if (!_show_overlay)
			osd_flags |= ImGuiWindowFlags_NoMove | ImGuiWindowFlags_NoInputs;
		// 오버레이 열림 시엔 SetNextWindowPos를 강제하지 않아 사용자가 드래그로 옮길 수 있게 함
		if (!_show_overlay)
			ImGui::SetNextWindowPos(fps_window_pos);
		else
			ImGui::SetNextWindowPos(fps_window_pos, ImGuiCond_Appearing);
		ImGui::PushStyleColor(ImGuiCol_Text, ImVec4(_fps_col[0], _fps_col[1], _fps_col[2], _fps_col[3]));
		ImGui::Begin("OSD", nullptr, osd_flags);
```
(기존 `ImGui::SetNextWindowPos(fps_window_pos);` 한 줄과 `ImGui::Begin("OSD", nullptr, <flags>)`를 위 형태로 교체.)

- [ ] **Step 2: 드래그된 위치를 비율로 저장**

OSD `ImGui::End();` 직전에, 오버레이 열림 시 현재 창 위치를 읽어 비율로 저장:
```cpp
		if (_show_overlay)
		{
			const ImVec2 wp = ImGui::GetWindowPos();
			const ImVec2 ws = ImGui::GetWindowSize();
			const float nx = (imgui_io.DisplaySize.x - ws.x) > 1.0f ? wp.x / (imgui_io.DisplaySize.x - ws.x) : 0.0f;
			const float ny = (imgui_io.DisplaySize.y - ws.y) > 1.0f ? wp.y / (imgui_io.DisplaySize.y - ws.y) : 0.0f;
			const float cnx = ImClamp(nx, 0.0f, 1.0f);
			const float cny = ImClamp(ny, 0.0f, 1.0f);
			if (fabsf(cnx - _sherbet_osd_x) > 0.001f || fabsf(cny - _sherbet_osd_y) > 0.001f)
			{
				_sherbet_osd_x = cnx;
				_sherbet_osd_y = cny;
				save_config();
			}
		}
```
주의: `ImGui::GetWindowPos/GetWindowSize`는 OSD 창 컨텍스트(Begin~End 사이)에서 호출해야 함. `save_config()`가 이 스코프에서 호출 가능한지 확인(runtime 멤버).

- [ ] **Step 3: 정독 + Commit(push 안 함)**

```bash
cd ~/reshade
git add source/runtime_gui.cpp
git commit -m "feat(sherbet): OSD 마우스 드래그 이동 + 위치 저장"
```

---

### Task 3: 플랜 CI 검증 (컨트롤러)
- [ ] push + CI watch. 실패 시 첫 에러 픽스 후 재검증.

---

## Self-Review 메모
- **커버리지:** 스펙 §7.1 OSD 위치(X/Y)·크기·드래그·표시항목(기존 show_clock/fps/frametime/preset_name 토글 유지). 표시항목 토글은 기존 설정에 이미 존재.
- **리스크:** OSD 텍스트 정렬 `_fps_pos % 2` → `osd_align_right` 교체 범위 한정(OSD 블록만). 드래그 시 SetNextWindowPos 조건 분기가 깜빡임 유발 안 하도록 Appearing 사용. save_config 빈번 호출 방지 위해 변화 임계값(0.001) 둠.
- **타입 일관성:** `_sherbet_osd_x/y` 멤버가 로드/저장/렌더/설정 슬라이더에서 일치.
